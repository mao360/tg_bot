version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: tg_bot
    restart: unless-stopped
    # Если нужно передать переменные окружения, раскомментируйте:
    # environment:
    #   - BOT_TOKEN=your_bot_token_here
    #   - DEBUG=true
    # Если нужно монтировать volume для логов, раскомментируйте:
    # volumes:
    #   - ./logs:/app/logs
    # Если нужно открыть порт, раскомментируйте:
    # ports:
    #   - "8080:8080"

